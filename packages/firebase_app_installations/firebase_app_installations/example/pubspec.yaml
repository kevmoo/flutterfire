name: firebase_app_installations_example
resolution: workspace
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  firebase_core: any
  firebase_app_installations: any
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: any

flutter:
  uses-material-design: true

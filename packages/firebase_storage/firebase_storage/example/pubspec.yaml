name: firebase_storage_example
description: Demonstrates how to use the firebase_storage plugin.
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: '>=3.27.0'

dependencies:
  firebase_core: ^4.6.0
  firebase_storage: ^13.2.0
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  image_picker_for_web: ^3.0.6
  web: ^1.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/hello.txt
